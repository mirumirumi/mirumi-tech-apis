AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: backend for mirumi.tech


Parameters:
  ResourcePrefix:
    Type: String
    Default: mirumitech
  EnvironmentName:
    Type: String
    AllowedValues:
      - dev
      - prd
  ApiAllowOrigin:
    Type: String


Resources:
  FunctionsStack:
    Type: AWS::Serverless::Application
    Properties:
      Location: templates/functions.yaml
      Parameters:
        ResourcePrefix: !Ref ResourcePrefix
        EnvironmentName: !Ref EnvironmentName
        ApiAllowOrigin: !Ref ApiAllowOrigin

  # タグ全部 samconfig.toml でついたか確認する（BffStackは消してないよ）

  # BffStack:
  #   Type: AWS::Serverless::Application
  #   Properties:
  #     Location: templates/bff.yaml
  #     Parameters:
  #       ResourcePrefix: !Ref ResourcePrefix
  #       EnvironmentName: !Ref EnvironmentName
  #     Tags:
  #       project: !Ref ResourcePrefix
  #       env: !Ref EnvironmentName
  #       IaC: cfn
